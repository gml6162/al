#include <stdio.h>
#include <iostream>
#define size 5
int get_average(int score[],int n);
void square_array(int a[], int siz);
void print_array(int a[], int siz);
int binary_searcy(int list[], int n, int key);
int main()
{
	/*int i;
	double sine[25], cosin[25], tangent[25];
	for(i=0; i<=24; i++)
	{
		sine[i]=sin((i*15)*3.141592/180.);
		cosin[i]=cos((i*15)*3.141592/180.);
		tangent[i]=tan((i*15)*3.141592/180.);
	}
	for(i=0;i<=24;i++)
	{printf("sine(%3d)=%5.2f\ncos(%3d)=%5.2f\ntan(%3d)=%5.2f\n",i*15, sine[i],i*15, cosin[i], i*15, tangent[i]);
	}
	int i, j, x[]={5, 5, 3, 4, 6, 8, 10, 1, 2, 3};
	int count=sizeof(x)/sizeof(x[0]);
	for(i=0; i<count;i++)
	{
		printf("%2d:", i);
		for (j=0; j<x[i];j++)
			printf("%c", '*');
		printf("\n");
	}
	int i, j, rank;
	int stud[10]={45, 93, 88, 95, 33, 54, 83, 29,56, 90};
	for(i=0;i<10;i++)
	{
		rank=0;
		for(j=0;j<10;j++)
			if(stud[i]<=stud[j])
				rank++;
		printf("%d:%d위\n", stud[i], rank);
	}
	int a[3];
	a[3]=55;
	printf("%d", a[3]+6);
	int seat[500]={0}, i;
	char  answer, ans1, ans2;
	printf("좌석을 예약하시겠습니까?");
	scanf("%c", &answer);
	if(answer=='n')
	{
		printf("안녕히 가십시오.");
		return 0;
	}
	else if(answer=='y')
	{
		printf("몇 자리를 예약하시겠습니까?");
		scanf("%d", &ans1);
		for(i=0;i<ans1;)
		{
		printf("좌석을 입력해주세요.");
		scanf("%d", &ans2);
		if(ans2<=0||ans2>500)
			{
				printf("다시 입력해주세요.\n");
				continue;
			}
		if(seat[ans2-1]==0)
		{
			seat[ans2-1]=1;
			printf("예약되었습니다.\n");
			i++;
		}
		else
			printf("이미 예약된 자리입니다.\n");
		}
		return 0;
	}
	int freq[5]={0};
	int candi;
	while(1)
	{
	printf("후보의 번호를 입력해세요.",i);
	rand();
	scanf("%d", &candi);
	if(candi<0)break;
	freq[candi]++
	}
	printf("결과\n");
	for(i=1;i<5;i++)
		printf("%3d%3d\n", i, freq[i]);
	return 0;
	
		int freq[30000], i;
		for(i=0;i<30000;i++);
		{
			freq[i]=rand();
		}
	int data[5];
	int i;
	for(i=0;i<5;i++)
	{
		printf("정수를 입력하시오:");
		scanf("%d", &data[i]);
	}
	for(i=4;i>=0;i--)
	{
		printf("%d\n", data[i];
	}
	int order[100];
	int i, sum1=0, sum2=0;
	for(i=0;i<100;i++)
	{
		order[i]=i;
		printf("%d\n", order[i]);
		if(order[i]%2==0)
			sum2+=order[i];
		else if(order[i]%2==1)
			sum1+=order[i];
	}
	for(i=99;i>=0;i--)
	{
		printf("%d\n", order[i]);
	}
	printf("홀수 합계:%d\n짝수 합계:%d\n", sum1, sum2);
int freq[6]={0};
	int i;
	for(i=0;i<10000;i++)
		++freq[rand()%6];//주사위를 굴려 나온 숫자를 +1
	printf("면 빈도\n");
	for(i=0;i<6;i++)
		printf("%3d %3d\n", i, freq[i]);
	int grade[5]={1, 2, 3, 4, 5};
	int avg;
	avg=get_average(grade, 5);
	printf("평균은 %d입니다.\n", avg);
}
int get_average(int score[], int n)
{
	int i;
	int sum=0;
	for(i=0;i<n;i++)
		sum+=score[i];
	return sum/n;
}
int list[]={3,2,9,7,1,4,8,0,6,5};
int i, j, temp, least;
for(i=0;i<9;i++)//10개의 숫자 정렬을 위해 아홉번
{
	least=i;//least-인덱스 번호, i-시작위치
	for(j=i+1;j<10;j++)//비교를 할 숫자들
		if(list[j]>list[least])//최솟값찾기
			least=j;
	temp=list[i];//임시저장공간으로 옮기기
	list[i]=list[least];//실제 최솟값 올바른 곳으로
	list[least]=temp;
}
for(i=0;i<10; i++)
	printf("%d ", list[i]);
printf("\n");
int list[]={2,6,11,13,18,20,22,27,29,34,38,41,42,45};
int key;
scanf("%d", &key);
printf("결과 : %d\n", binary_searcy(list,14,key));
}*/
	int s[5][5];
	int i, j;
	int value=0;
	for(i=0;i<5;i++)
		for(j=0;j<5;j++)
		s[i][j]=value++;
	for(i=0;i<5;i++)
		for(j=0;j<5;j++)
			printf("%d\n", s[i][j]);
}
